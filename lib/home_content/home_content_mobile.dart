import 'package:flutter/material.dart';
import 'package:myportofolio/Info.dart';

class HomeContentMobile extends StatelessWidget {
  const HomeContentMobile({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return InfoDetails();
  }
}